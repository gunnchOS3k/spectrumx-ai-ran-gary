# Artifact evaluation (Paper II)

Judged SpectrumX core stays the competition artifact.

Digital programme: `paper/artifacts/experiment_protocol.yaml` (frozen, committed first) + `scripts/run_paper_ii_heldout.py` + `paper/scripts/generate_tables.py`.

Tables in `paper/tables/` are `SYNTHETIC_SIM`. Compute columns are `HOST_PROCESS_TIMING`. ReadyGary sibling tables are FR2 28 GHz, not Sub-6.

Independent reproduction by an outside lab is still pending (`docs/packets/EXTERNAL_REPRODUCTION_PACKET.md`).
