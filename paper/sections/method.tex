\section{Method}
Baselines first: uniform/rule-based allocation, then fairness-aware policy on synthetic snapshots (\texttt{scripts/demo\_airan\_policy.py --toy}).
Learning-based controllers are out of scope until information-equivalent comparisons, seeds, CIs, ablations, domain shift, and switching/compute cost are logged.
28~GHz in ReadyGary is FR2 mmWave~\cite{3gpp_ts38101_2}; DeepMIMO-style generators if used are cited~\cite{alkhateeb2019deepmimo} and remain synthetic unless a dataset provenance file says otherwise.
Pre-registration: \texttt{paper/artifacts/PREREGISTRATION.md}.
